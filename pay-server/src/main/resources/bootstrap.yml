
spring:
  cloud:
    config:
      uri: http://localhost:8099/
      label: master
      profile: dev
  application:
    name: pay-server


management:
  endpoints:
    web:
      exposure:
        exclude: refresh,health,info
server:
  port: 8761


