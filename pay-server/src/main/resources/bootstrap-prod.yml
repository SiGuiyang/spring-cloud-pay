
spring:
  cloud:
    config:
      uri: http://localhost:8099/
      label: master
      profile: prod
  application:
    name: pay-server


management:
  endpoints:
    web:
      exposure:
        exclude: refresh,health,info
server:
  port: 8761


