
spring:
  cloud:
    config:
      uri: http://localhost:8099/
      label: master
      profile: dev
  application:
    name: pay-admin


management:
  endpoints:
    web:
      exposure:
        exclude: refresh,health,info